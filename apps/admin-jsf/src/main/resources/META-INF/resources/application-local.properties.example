# Copie este arquivo para "application-local.properties" (NAO versionado)
# e preencha com os mesmos dados usados em apps/api-java (mesmo banco).

spring.datasource.url=jdbc:postgresql://SEU_HOST:5432/SEU_BANCO
spring.datasource.username=SEU_USUARIO
spring.datasource.password=SUA_SENHA

# Descomente se o host tiver "pooler" no nome (Supabase Supavisor)
# spring.datasource.hikari.data-source-properties.prepareThreshold=0