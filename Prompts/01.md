# REDESIGN VISUAL DO DASHBOARD DOKEI — EXECUÇÃO COMPLETA

## CONTEXTO DO PROJETO

Você está trabalhando no **Dokei** (dokei.com.br), um SaaS de gestão para MEIs brasileiros.

**Stack:**

- Next.js 14 + TypeScript + Turborepo + pnpm
- Supabase (auth + banco) + Prisma v5
- Tailwind CSS v4 (configuração via `@theme` no globals.css, NÃO existe `tailwind.config.ts`)
- shadcn/ui + Lucide icons
- Localização: `C:\Users\dougl\OneDrive\programacao\dokei` (raiz do monorepo)
- Pasta de trabalho: `apps\web` (todos os caminhos abaixo são relativos a ela)

**Design system existente em `apps\web\src\app\globals.css`:**
Já existem tokens semânticos completos: `--background`, `--foreground`, `--card`, `--primary`, `--secondary`, `--muted`, `--muted-foreground`, `--accent`, `--destructive`, `--border`, `--input`, `--ring`, `--sidebar`, `--sidebar-foreground`, `--sidebar-primary`, `--sidebar-primary-foreground`, `--sidebar-accent`, `--sidebar-border`. Modo dark já configurado via `.dark` class. Cor primária: `#1a4d2e` (verde escuro).

---

## REGRAS DE PROCESSO — APLICAR SEMPRE

1. **NUNCA criar código sem antes ler o que já existe.** Antes de qualquer alteração, leia o arquivo completo.
2. **SEMPRE listar possíveis causas do erro antes de propor solução.**
3. **SEMPRE analisar o código existente e construir em cima do que já foi projetado.**
4. **NUNCA usar `any`** — sempre tipo nomeado específico.
5. **NUNCA usar emoji no código** — apenas ícones Lucide/shadcn.
6. **SEMPRE incluir passo a passo de implementação.**
7. **SEMPRE fornecer comandos PowerShell prontos para colar** (Windows + VS Code + PowerShell).
8. **SEMPRE especificar o caminho do arquivo** antes de cada bloco de código (ex: `apps\web\src\components\...`).
9. **SEMPRE mudar APENAS a parte do código necessária** — nunca reescreva o arquivo inteiro a não ser que esteja estruturalmente quebrado.
10. **MUDE APENAS estilização visual** (classes Tailwind, CSS) — NÃO altere lógica, hooks, server actions, fetches, schemas Prisma, validações Zod, imports de função, props funcionais.

---

## OBJETIVO

Redesenhar visualmente o dashboard do Dokei (área logada) com:

- **Sidebar fixa à esquerda** com navegação para as 5 features (Recibos, Financeiro, Obrigações, Calculadora, Rendimentos) + item Início
- **Modo Light + Dark com toggle** no header
- **Cards com sombra suave** (estilo Stripe/Linear) na página inicial do dashboard
- **Tokens semânticos do design system** (`bg-background`, `text-foreground`, `bg-sidebar`, etc.) — NÃO usar cores hardcoded como `bg-gray-50`, `text-gray-900`, `[#1B5E20]`
- Tipografia clara, espaçamento generoso, micro-interações sutis (hover, transition)
- Profissional, agradável, NÃO cansativo para a visão

---

## ARQUIVOS QUE VOCÊ VAI MEXER OU CRIAR

| Ação     | Arquivo                                               |
| -------- | ----------------------------------------------------- |
| Instalar | dependência `next-themes`                             |
| CRIAR    | `apps\web\src\components\providers\ThemeProvider.tsx` |
| EDITAR   | `apps\web\src\app\layout.tsx` (root)                  |
| CRIAR    | `apps\web\src\components\dashboard\Sidebar.tsx`       |
| CRIAR    | `apps\web\src\components\dashboard\ThemeToggle.tsx`   |
| EDITAR   | `apps\web\src\app\(dashboard)\layout.tsx`             |
| EDITAR   | `apps\web\src\app\(dashboard)\dashboard\page.tsx`     |

---

## TAREFA — EXECUÇÃO EM 6 ETAPAS

### ETAPA 1 — Instalar `next-themes`

Execute no terminal (dentro de `apps\web`):

```powershell
pnpm add next-themes
```

Confirme no `package.json` que `next-themes` aparece em `dependencies`.

---

### ETAPA 2 — Criar `ThemeProvider`

📄 `apps\web\src\components\providers\ThemeProvider.tsx` — **CRIAR**

```tsx
"use client"

import { ThemeProvider as NextThemesProvider } from "next-themes"
import { type ThemeProviderProps } from "next-themes"

export function ThemeProvider({ children, ...props }: ThemeProviderProps) {
  return <NextThemesProvider {...props}>{children}</NextThemesProvider>
}
```

---

### ETAPA 3 — Envolver root layout com ThemeProvider

📄 `apps\web\src\app\layout.tsx` — **EDITAR (3 mudanças mínimas)**

**Antes de editar, leia o arquivo inteiro.**

**Mudança A** — Adicionar import junto aos outros imports do topo:

```tsx
import { ThemeProvider } from "@/components/providers/ThemeProvider"
```

**Mudança B** — Na tag `<html>`, adicionar `suppressHydrationWarning`:

De:

```tsx
<html lang="pt-BR" className="scroll-smooth bg-background">
```

Para:

```tsx
<html lang="pt-BR" className="scroll-smooth bg-background" suppressHydrationWarning>
```

**Mudança C** — Envolver `{children}` e `<ChatWidget />` com `<ThemeProvider>`:

De:

```tsx
<body className={`${montserrat.variable} font-sans antialiased`}>
  {children}
  <ChatWidget />
</body>
```

Para:

```tsx
<body className={`${montserrat.variable} font-sans antialiased`}>
  <ThemeProvider
    attribute="class"
    defaultTheme="system"
    enableSystem
    disableTransitionOnChange
  >
    {children}
    <ChatWidget />
  </ThemeProvider>
</body>
```

NÃO altere mais nada nesse arquivo.

---

### ETAPA 4 — Criar Sidebar

📄 `apps\web\src\components\dashboard\Sidebar.tsx` — **CRIAR**

```tsx
"use client"

import Link from "next/link"
import { usePathname } from "next/navigation"
import {
  LayoutDashboard,
  FileText,
  TrendingUp,
  Bell,
  Calculator,
  BarChart2,
} from "lucide-react"

interface NavItem {
  label: string
  href: string
  icon: React.ComponentType<{ size?: number; className?: string }>
}

interface SidebarProps {
  userEmail: string
}

const navItems: NavItem[] = [
  { label: "Início", href: "/dashboard", icon: LayoutDashboard },
  { label: "Recibos", href: "/recibos", icon: FileText },
  { label: "Financeiro", href: "/financeiro", icon: TrendingUp },
  { label: "Obrigações", href: "/obrigacoes", icon: Bell },
  { label: "Calculadora", href: "/calculadora-preco", icon: Calculator },
  { label: "Rendimentos", href: "/rendimentos", icon: BarChart2 },
]

export function Sidebar({ userEmail }: SidebarProps) {
  const pathname = usePathname()

  return (
    <aside className="fixed left-0 top-0 h-screen w-64 bg-sidebar border-r border-sidebar-border flex flex-col">
      <div className="px-6 py-6 border-b border-sidebar-border">
        <Link href="/dashboard" className="flex items-center gap-2">
          <div className="w-8 h-8 rounded-lg bg-sidebar-primary flex items-center justify-center">
            <span className="text-sidebar-primary-foreground font-bold text-sm">
              D
            </span>
          </div>
          <span className="text-lg font-bold text-sidebar-foreground">
            Dokei
          </span>
        </Link>
      </div>

      <nav className="flex-1 px-3 py-4 space-y-1 overflow-y-auto">
        {navItems.map((item) => {
          const Icon = item.icon
          const ativo =
            pathname === item.href ||
            (item.href !== "/dashboard" && pathname.startsWith(item.href))

          return (
            <Link
              key={item.href}
              href={item.href}
              className={`
                flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm font-medium
                transition-all duration-200
                ${
                  ativo
                    ? "bg-sidebar-primary text-sidebar-primary-foreground shadow-sm"
                    : "text-sidebar-foreground/80 hover:bg-sidebar-accent/10 hover:text-sidebar-foreground"
                }
              `}
            >
              <Icon size={18} />
              {item.label}
            </Link>
          )
        })}
      </nav>

      <div className="px-4 py-4 border-t border-sidebar-border">
        <div className="flex items-center gap-3 px-2">
          <div className="w-9 h-9 rounded-full bg-sidebar-accent/20 flex items-center justify-center flex-shrink-0">
            <span className="text-sidebar-foreground text-sm font-semibold">
              {userEmail.charAt(0).toUpperCase()}
            </span>
          </div>
          <div className="min-w-0 flex-1">
            <p className="text-xs text-sidebar-foreground/60">Logado como</p>
            <p className="text-sm font-medium text-sidebar-foreground truncate">
              {userEmail}
            </p>
          </div>
        </div>
      </div>
    </aside>
  )
}
```

---

### ETAPA 5 — Criar ThemeToggle

📄 `apps\web\src\components\dashboard\ThemeToggle.tsx` — **CRIAR**

```tsx
"use client"

import { useTheme } from "next-themes"
import { Moon, Sun } from "lucide-react"
import { useEffect, useState } from "react"

export function ThemeToggle() {
  const { theme, setTheme } = useTheme()
  const [montado, setMontado] = useState(false)

  useEffect(() => {
    setMontado(true)
  }, [])

  if (!montado) {
    return <div className="w-9 h-9 rounded-lg bg-muted/50" aria-hidden="true" />
  }

  const escuro = theme === "dark"

  return (
    <button
      type="button"
      onClick={() => setTheme(escuro ? "light" : "dark")}
      aria-label={escuro ? "Ativar modo claro" : "Ativar modo escuro"}
      className="
        cursor-pointer w-9 h-9 rounded-lg flex items-center justify-center
        text-muted-foreground hover:text-foreground hover:bg-muted/50
        transition-colors
      "
    >
      {escuro ? <Sun size={18} /> : <Moon size={18} />}
    </button>
  )
}
```

---

### ETAPA 6 — Atualizar Layout do Dashboard

📄 `apps\web\src\app\(dashboard)\layout.tsx` — **EDITAR (apenas estilização)**

**Antes de editar, leia o arquivo inteiro.** Lógica de auth (`createClient`, `supabase.auth.getUser()`, `redirect`) e Server Action `logout` ficam INTOCADAS.

**Mudança A** — Adicionar 3 imports junto aos imports já existentes:

```tsx
import { Sidebar } from "@/components/dashboard/Sidebar"
import { ThemeToggle } from "@/components/dashboard/ThemeToggle"
import { LogOut } from "lucide-react"
```

**Mudança B** — Substituir TODO o bloco `return (...)` por:

```tsx
return (
  <div className="min-h-screen bg-background text-foreground">
    <div className="flex">
      <Sidebar userEmail={user.email ?? ""} />

      <div className="flex-1 ml-64">
        <header className="sticky top-0 z-30 bg-background/80 backdrop-blur-md border-b border-border">
          <div className="px-8 py-4 flex items-center justify-end gap-4">
            <ThemeToggle />

            <div className="h-6 w-px bg-border" />

            <span className="text-sm text-muted-foreground hidden sm:inline">
              {user.email}
            </span>

            <form action={logout}>
              <button
                type="submit"
                className="cursor-pointer text-sm font-medium text-muted-foreground hover:text-destructive transition-colors flex items-center gap-1.5"
              >
                <LogOut size={16} />
                Sair
              </button>
            </form>
          </div>
        </header>

        <main className="px-8 py-8 max-w-6xl">{children}</main>
      </div>
    </div>
  </div>
)
```

NÃO altere mais nada nesse arquivo.

---

### ETAPA 7 — Refinar Dashboard Page (cards estilo Stripe)

📄 `apps\web\src\app\(dashboard)\dashboard\page.tsx` — **EDITAR (apenas estilização)**

**Antes de editar, leia o arquivo inteiro.** Lógica do `createClient`, `supabase.auth.getUser()`, array `funcionalidades` e tipo `FuncionalidadeCard` ficam INTOCADOS.

**Mudança A** — Substituir o JSX do `return` da função `DashboardPage` por:

```tsx
return (
  <div className="space-y-10">
    <div>
      <h1 className="text-3xl font-bold text-foreground tracking-tight">
        Olá, {primeiroNome}!
      </h1>
      <p className="text-muted-foreground mt-2 text-base">
        O que você precisa fazer hoje?
      </p>
    </div>

    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
      {funcionalidades.map((item) => (
        <Card key={item.href} {...item} />
      ))}
    </div>
  </div>
)
```

**Mudança B** — Substituir TODO o componente interno `Card` por:

```tsx
function Card({
  titulo,
  descricao,
  href,
  icon: Icon,
  disponivel,
}: FuncionalidadeCard) {
  if (!disponivel) {
    return (
      <div className="group relative p-6 bg-card rounded-xl border border-border opacity-60 cursor-not-allowed">
        <div className="w-11 h-11 bg-muted rounded-xl flex items-center justify-center">
          <Icon size={20} className="text-muted-foreground" />
        </div>
        <h2 className="text-base font-semibold text-card-foreground mt-4">
          {titulo}
        </h2>
        <p className="text-sm text-muted-foreground mt-1.5 leading-relaxed">
          {descricao}
        </p>
        <span className="inline-block mt-4 text-xs font-medium text-muted-foreground bg-muted px-2.5 py-1 rounded-full">
          Em breve
        </span>
      </div>
    )
  }

  return (
    <Link
      href={href}
      className="
        group relative p-6 bg-card rounded-xl border border-border
        shadow-sm hover:shadow-md
        hover:border-primary/30 hover:-translate-y-0.5
        transition-all duration-200
      "
    >
      <div className="w-11 h-11 bg-primary/10 rounded-xl flex items-center justify-center group-hover:bg-primary/15 transition-colors">
        <Icon size={20} className="text-primary" />
      </div>
      <h2 className="text-base font-semibold text-card-foreground mt-4">
        {titulo}
      </h2>
      <p className="text-sm text-muted-foreground mt-1.5 leading-relaxed">
        {descricao}
      </p>
    </Link>
  )
}
```

NÃO altere mais nada nesse arquivo.

---

## VALIDAÇÃO FINAL

Após aplicar TODAS as etapas:

1. Suba o dev server: `pnpm dev` (na raiz do monorepo)
2. Acesse `http://localhost:3000/dashboard` (logado)
3. Confirme que:
   - Sidebar fixa apareceu à esquerda com 6 itens (Início + 5 features)
   - Item ativo da sidebar fica destacado em verde
   - Toggle dark/light no header funciona e troca o tema instantaneamente
   - Cards têm sombra suave e elevação sutil no hover
   - Não há erros no console do navegador
   - Não há erros de TypeScript no terminal
4. Rode `pnpm tsc --noEmit` (na pasta `apps\web`) para confirmar tipagem sem erros.

---

## CHECKLIST DE QUALIDADE — VERIFICAR ANTES DE FINALIZAR

- [ ] Nenhum `any` foi introduzido
- [ ] Nenhum emoji foi inserido em código
- [ ] Nenhuma lógica de auth/Supabase/Prisma foi alterada
- [ ] Nenhum `bg-gray-*`, `text-gray-*` ou `[#hexadecimal]` foi adicionado nos arquivos editados (apenas tokens semânticos: `bg-background`, `text-foreground`, `bg-card`, `bg-sidebar`, `text-primary`, `border-border`, etc.)
- [ ] Cada novo arquivo tem o caminho correto especificado
- [ ] Apenas as 7 etapas acima foram executadas, nada além
- [ ] `pnpm tsc --noEmit` passa sem erros
- [ ] Dashboard funciona em modo claro E escuro

---

## EM CASO DE ERRO

Se algo quebrar:

1. **Liste todas as possíveis causas antes de propor solução.**
2. Mostre o erro completo com stack trace.
3. Identifique qual arquivo causou.
4. Proponha correção mínima — apenas a linha problemática.

EXECUTE AGORA, NA ORDEM, AS 7 ETAPAS.
